When a child Sim ages-up to Teen, if either parent have values defined in this Custom Definition then they will automatically be copied to the child based on this percentage.  100 = always use parent parts, 0 = never use parent parts.